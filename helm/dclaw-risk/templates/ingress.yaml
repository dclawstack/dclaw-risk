apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: {{ include "dclaw-risk.name" . }}
  labels:
    app.kubernetes.io/name: {{ include "dclaw-risk.name" . }}
spec:
  rules:
    - host: dclaw-risk.local
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: {{ include "dclaw-risk.name" . }}
                port:
                  number: {{ .Values.frontend.service.port }}
